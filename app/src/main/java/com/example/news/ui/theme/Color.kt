package com.example.news.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Application color palette used by [DarkColorScheme] and [LightColorScheme] in `Theme.kt`.
 *
 * The "80" variants are lighter tones intended for dark-theme surfaces, while the "40"
 * variants are deeper tones used in the light theme. These colours serve as the primary,
 * secondary, and tertiary slots in the Material 3 colour scheme.
 */

/** Light purple tone for dark-theme primary color. */
val Purple80 = Color(0xFFD0BCFF)

/** Light purple-grey tone for dark-theme secondary color. */
val PurpleGrey80 = Color(0xFFCCC2DC)

/** Light pink tone for dark-theme tertiary color. */
val Pink80 = Color(0xFFEFB8C8)

/** Deep purple tone for light-theme primary color. */
val Purple40 = Color(0xFF6650a4)

/** Deep purple-grey tone for light-theme secondary color. */
val PurpleGrey40 = Color(0xFF625b71)

/** Deep pink tone for light-theme tertiary color. */
val Pink40 = Color(0xFF7D5260)